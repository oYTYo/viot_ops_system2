请严格按照以下指令修改代码，不要自行发散：
- 目标文件：[请替换为统计分析板块对应的组件路径，例如 src/components/StatisticalAnalysis/index.tsx]
- 修改位置：组件的主渲染(Render)部分、样式定义部分及事件处理函数。
- 修改逻辑：
1. 【整体布局重构】：将包裹“统计分析”板块的最外层容器样式修改为四宫格布局。使用 CSS Grid：`display: grid; grid-template-columns: 1fr 1fr; grid-template-rows: 1fr 1fr; gap: 16px;`（gap可根据实际UI规范微调）。将原有的四个子模块分别放入四个 Grid Area 中。

2. 【第一象限（黄金指标）】：保留现有逻辑与组件，放入第一象限。

3. 【第二象限（资源状态统计）】：
   - 样式隔离：务必仅在当前组件内增加特定的 className 或使用 scoped 样式，绝对不能修改公用的状态统计组件样式（避免污染运维大屏）。
   - 布局微调：从横向改为纵向 Flex 布局（`flex-direction: column`）。
   - 内容重构：
     - 第1行：新增公共图例（Legend），横向排列显示：“正常(对应的颜色标识)”、“异常(颜色)”、“断连(颜色)”。
     - 第2/3/4行：分别渲染摄像机、流链路、服务器的统计条。移除它们原有的状态 Icon 和名称文本，仅保留“状态色条”和“数量数值”。确保这三行紧凑排列。

4. 【第三象限（根因实体统计）】：
   - 将原有按钮的文本从“完整表格”修改为“详情”。
   - 修改该按钮的 onClick 事件，将其置空或仅阻止默认行为（不执行任何跳转或展开操作）。
   - 直接在该象限的 DOM 内渲染完整数据列表/表格。
   - 为包裹数据的容器添加样式：`overflow-y: auto;` 并限制合适的最大高度（如 `max-height: 100%;`），使其在当前宫格内支持下拉滚动，不撑开父级容器。

5. 【第四象限（告警类型统计）】：
   - 将原有按钮的文本从“完整表格”修改为“详情”。
   - 修改该按钮的 onClick 事件，使其不执行任何操作（同第三象限）。